# Дизайн-система «Новата Инвест» v3 — «Институциональный»

Один файл — `assets/novata.css`. Он не знает ничего о конкретных страницах:
только токены, типографика, кнопки, поверхности и поля формы. Вся раскладка
страниц живёт отдельно, в `assets/site.css`.

## Направление

Язык крупных управляющих компаний: тёплая бумага вместо белого, крупная
антиква тонкого начертания в заголовках, волосяные линейки вместо карточек с
тенями, нулевые скругления, один чернильно-синий акцент. Ничего не «всплывает»
и не пульсирует — спокойствие здесь и есть аргумент.

Три правила, из которых выводится всё остальное:

1. **Линейка вместо карточки.** Блок отделяется от соседа линией и воздухом, а
   не рамкой, фоном и тенью. Тень в системе есть только у модального окна.
2. **Антиква говорит, гротеск объясняет.** Spectral — заголовки, крупные числа,
   контакты, названия. IBM Plex Sans — всё, что читают подряд.
3. **Один акцент.** `#1B3A6B` отмечает действие и ничего больше. Второго
   акцентного цвета в системе нет.

## Что взято из старой вёрстки

| Наследие | Было | Стало |
| --- | --- | --- |
| Тёмно-синий фон подвала | `#051A3B` | `--nv-ink` `#10141C` — чернила, тёмные полосы |
| Акцентный синий | `#1E5FE0` | `--nv-accent` `#1B3A6B` — приглушён до чернильного |
| Белый фон | `#FFFFFF` | `--nv-paper` `#F7F5F0` — тёплая бумага; белый остался для плашек |
| Шрифт | Manrope | Spectral + IBM Plex Sans |
| Ширина макета | 1200 px | 1280 px |

Структура разделов, все тексты и юридические документы не менялись.

## Слои

```
1. Токены        поверхности, чернила, линейки, акцент, шрифт, размеры, движение
2. База          reset, body, фокус, выделение
3. Раскладка     .container, .section, .rule, .grid
4. Типографика   .display .h1 .h2 .h3 .lead .small .fine .eyebrow
5. Кнопки        .btn + 4 варианта, .link-arrow, .icon-btn
6. Поверхности   .plate .recess .marker .ledger
7. Поля формы    .field .input .check .link-legal
8. Движение      [data-reveal], prefers-reduced-motion
9. Утилиты       .visually-hidden, сетки
```

## Цвет

Поверхности: `--nv-paper` `#F7F5F0`, `--nv-plate` `#FFFFFF`,
`--nv-recess` `#EEEAE1`.
Чернила: `--nv-ink` `#10141C`, `--nv-ink-2` `#4A4F5A`, `--nv-ink-3` `#6B6257`.
Линейки: `--nv-rule` `#DAD5CB` (декоративная), `--nv-rule-strong` `#10141C`
(акцентная над блоком), `--nv-rule-control` `#8C8578` (граница поля — несёт
смысл, поэтому 3.5:1).
Акцент: `--nv-accent` `#1B3A6B`, наведение `#0E2547`.
На тёмном: `--nv-on-ink` `#F7F5F0`, `-2` `#B9B5AC`, `-3` `#8D8A83`,
линейка `#333844`, ссылка `#C9D4E8`.
Состояние ошибки: `--nv-danger` `#8E2C20`, на тёмном `#E39A8E`.

Компоненты читают семантический слой, а не эти шкалы:

```css
--bg --fg --fg-muted --fg-subtle --rule --accent --accent-fg --danger
```

Класс `.on-ink` переворачивает семантику для тёмных полос (блок заявки,
контакты, дисклеймер, подвал юридических страниц) — те же `.btn--outline`,
`.eyebrow`, `.link-arrow` работают без отдельных правил.

Одно исключение: `.form-card` — светлая карточка внутри тёмной полосы — сама
возвращает светлую семантику. Без этого ссылки «Обработку персональных данных»
и «Политикой конфиденциальности» наследовали бы цвет тёмного фона и пропадали
на бумаге.

## Типографика

```
--t-display  2.40 → 4.25 rem   Spectral 300   первый экран
--t-h1       2.00 → 3.40 rem   Spectral 300   заголовок внутренней страницы
--t-h2       1.80 → 2.90 rem   Spectral 300   заголовок секции
--t-h3       1.15 → 1.35 rem   Spectral 600   карточка, шаг, статья
--t-lead     1.05 → 1.20 rem   Plex Sans 400  подзаголовок
--t-body     1.00 rem          Plex Sans 400  текст
--t-sm       0.94 rem          Plex Sans 400  описания, поля ввода
--t-xs       0.81 rem          Plex Sans 400  подписи, дисклеймеры
--t-2xs      0.75 rem          Plex Sans 600  надзаголовки, метки полей
```

Крупные кегли — вес 300 с трекингом `-0.015em`. Надзаголовок `.eyebrow` —
верхний регистр, трекинг `0.14em`, слева короткая линейка. Заголовкам
`text-wrap: balance`, ведущим абзацам `pretty`.

## Форма и размеры

Отступы кратны 4 px (`--s-1` … `--s-32`). Скругления — ноль, везде: кнопки,
поля, плашки, модальное окно. Единственные круглые элементы в системе —
точка в `.marker` и стрелка в `.link-arrow`.

Тени нет ни у одного блока, кроме модального окна. Глубина показывается
линейкой и фоном: `--nv-recess` утоплено, `--nv-plate` выступает.

## Кнопки

| Вариант | Где |
| --- | --- |
| `.btn--primary` | главное действие: «Получить консультацию», «Отправить» |
| `.btn--ink` | действие на бумаге, когда синий занят соседом |
| `.btn--outline` | вторичное действие; подстраивается под `.on-ink` |
| `.btn--paper` | действие на тёмной полосе |
| `.link-arrow` | текстовая ссылка-действие внутри блока |

Высота 56 px, `.btn--sm` 46, `.btn--lg` 62. Ширина — по содержимому или
`.btn--block`.

## Движение

Три длительности: `.16s` (отклик), `.28s` (смена состояния), `.55s` (панель,
модальное окно). Блоки появляются сдвигом на 14 px — меньше, чем в прежней
версии: в институциональном языке движение должно быть почти незаметным.
`prefers-reduced-motion: reduce` выключает его целиком.

## Доступность

- Фокус всегда виден: контур `--accent` с отступом 3 px.
- Контраст текста не ниже AA на своих подложках: `--fg` 18.8:1,
  `--fg-muted` 7.5:1, `--fg-subtle` 5.5:1 на бумаге; бумага на акценте 10.3:1;
  на тёмной полосе `--fg-muted` 9.0:1, `--fg-subtle` 5.4:1.
- Граница поля ввода `--nv-rule-control` — 3.5:1: она несёт смысл, в отличие
  от декоративной `--nv-rule`.
- У полей есть видимые метки, ошибки помечаются `aria-invalid` и текстом.
- Модальные окна: `role="dialog"`, `aria-modal`, Esc, клик мимо, возврат фокуса.
- Минимальная высота интерактивного элемента 46 px, у основных кнопок 56.

## Как расширять

1. Новый оттенок — сначала в шкалу, потом при необходимости в семантический
   слой. Hex внутри компонента не пишут.
2. Новый размер — из шкалы `--s-*`.
3. Перед новым компонентом проверьте, не собирается ли он из `.plate`,
   `.ledger`, `.rule` и `.btn`.
4. Тёмный фон — класс `.on-ink` на контейнер, а не отдельные правила.
5. Скругление, тень и второй акцентный цвет — не «ещё один вариант», а выход
   из направления. Если они понадобились, обсуждайте направление целиком.
